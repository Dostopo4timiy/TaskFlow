# Асинхронный сервис управления задачами

Асинхронный микросервис для управления задачами с использованием FastAPI, PostgreSQL, RabbitMQ и Docker.

## Функциональные возможности

### Основные возможности
- ✅ Создание задач через REST API
- ✅ Асинхронная обработка задач в фоновом режиме
- ✅ Параллельная обработка нескольких задач
- ✅ Система приоритетов для задач (LOW, MEDIUM, HIGH)
- ✅ Пагинация и фильтрация списка задач
- ✅ Мониторинг статуса выполнения задач
- ✅ Отмена выполняемых задач

### Статусная модель задач
- `NEW` - новая задача
- `PENDING` - ожидает обработки в очереди
- `IN_PROGRESS` - в процессе выполнения
- `COMPLETED` - завершено успешно
- `FAILED` - завершено с ошибкой
- `CANCELLED` - отменено пользователем

### Атрибуты задачи
- Уникальный идентификатор
- Название задачи
- Описание
- Приоритет (LOW, MEDIUM, HIGH)
- Статус выполнения
- Время создания
- Время начала выполнения
- Время завершения
- Результат выполнения
- Информация об ошибках (если есть)

## Технологический стек

- **Python 3.10+** - язык программирования
- **FastAPI** - асинхронный веб-фреймворк
- **PostgreSQL 14+** - реляционная база данных
- **RabbitMQ** - очередь сообщений для обработки задач
- **SQLAlchemy 2.0+** - асинхронный ORM
- **Alembic** - система миграций базы данных
- **Docker & Docker Compose** - контейнеризация
- **Pytest** - фреймворк для тестирования
- **Poetry** - управление зависимостями

## Структура проекта

```
task-service/
├── src/
│   ├── api/
│   │   ├── dependencies.py          # Зависимости FastAPI
│   │   └── v1/
│   │       ├── endpoints.py         # API endpoints
│   │       └── schemas.py           # Pydantic схемы
│   ├── core/
│   │   ├── config.py               # Конфигурация приложения
│   │   └── database.py             # Настройка БД
│   ├── models/
│   │   └── task.py                 # SQLAlchemy модели
│   ├── services/
│   │   ├── task_service.py         # Бизнес-логика задач
│   │   └── worker.py               # Обработчик задач из очереди
│   ├── worker.py                   # Точка входа для воркера
│   └── main.py                     # Точка входа для API
├── alembic/
│   └── versions/                   # Миграции базы данных
├── tests/
│   ├── conftest.py                 # Фикстуры pytest
│   ├── test_api.py                 # Тесты API
│   └── test_services.py            # Тесты сервисов
├── .env.example                    # Пример переменных окружения
├── docker-compose.yml              # Docker Compose конфигурация
├── Dockerfile                      # Docker образ
├── pyproject.toml                  # Конфигурация Poetry
├── requirements.txt                # Зависимости (для pip)
└── README.md                       # Документация
```

## Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Python 3.10+ (для локальной разработки)

### Запуск через Docker Compose

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd task-service
```

2. **Настройте переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env при необходимости
```

3. **Запустите приложение:**
```bash
docker-compose up --build
```

4. **Приложение будет доступно по адресам:**
   - API: http://localhost:8000
   - Документация OpenAPI: http://localhost:8000/docs
   - RabbitMQ Management: http://localhost:15672 (логин: taskuser, пароль: taskpass)
   - PostgreSQL: localhost:5432

### Локальная разработка

1. **Установите зависимости через Poetry:**
```bash
poetry install
```

2. **Активируйте виртуальное окружение:**
```bash
poetry shell
```

3. **Запустите базу данных и RabbitMQ:**
```bash
docker-compose up db rabbitmq -d
```

4. **Примените миграции:**
```bash
alembic upgrade head
```

5. **Запустите API сервер:**
```bash
uvicorn src.main:app --reload
```

6. **Запустите воркера (в отдельном терминале):**
```bash
python -m src.worker
```

## API Endpoints

### 1. Создание задачи
```http
POST /api/v1/tasks/
Content-Type: application/json

{
    "name": "Процесс отчетности",
    "description": "Генерация ежемесячного отчета",
    "priority": "HIGH"
}
```

### 2. Получение списка задач
```http
GET /api/v1/tasks/
Query Parameters:
- status (опционально): NEW, PENDING, IN_PROGRESS, COMPLETED, FAILED, CANCELLED
- priority (опционально): LOW, MEDIUM, HIGH
- page (опционально): номер страницы (по умолчанию: 1)
- size (опционально): размер страницы (по умолчанию: 10, максимум: 100)
```

### 3. Получение информации о задаче
```http
GET /api/v1/tasks/{task_id}
```

### 4. Отмена задачи
```http
DELETE /api/v1/tasks/{task_id}
```

### 5. Получение статуса задачи
```http
GET /api/v1/tasks/{task_id}/status
```

## Тестирование

### Запуск тестов
```bash
# Запуск всех тестов
pytest

# Запуск с покрытием
pytest --cov=src tests/

# Запуск конкретного тестового файла
pytest tests/test_api.py
```

### Типы тестов
- **Unit-тесты**: тестирование отдельных компонентов
- **Интеграционные тесты**: тестирование взаимодействия компонентов
- **API тесты**: тестирование HTTP endpoints

## Конфигурация

Основные настройки через переменные окружения (см. `.env.example`):

```env
# Database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dbname

# RabbitMQ
RABBITMQ_URL=amqp://user:password@localhost:5672/

# Application
LOG_LEVEL=INFO
WORKERS_NUM=3
TASK_TIMEOUT_SECONDS=300
```

## Миграции базы данных

### Создание новой миграции
```bash
alembic revision --autogenerate -m "Описание изменений"
```

### Применение миграций
```bash
alembic upgrade head
```

### Откат миграции
```bash
alembic downgrade -1
```

## Docker

### Сборка образа
```bash
docker build -t task-service .
```

### Запуск контейнера
```bash
docker run -p 8000:8000 --env-file .env task-service
```

## 📊 Мониторинг и логи

### Логирование
- Уровень логирования настраивается через \`LOG_LEVEL\`
- Логи выводятся в stdout в формате JSON

### Health check
```http
GET /health
```

## Архитектура

### Компоненты системы
1. **API Service** (FastAPI) - обработка HTTP запросов
2. **Task Worker** - обработка задач из очереди
3. **PostgreSQL** - хранение состояния задач
4. **RabbitMQ** - очередь сообщений с приоритетами

### Поток данных
1. Пользователь создает задачу через API
2. Задача сохраняется в БД со статусом \`NEW\`
3. Задача помещается в RabbitMQ очередь с приоритетом
4. Воркер получает задачу из очереди
5. Воркер обновляет статус на \`IN_PROGRESS\` и выполняет задачу
6. По завершении статус обновляется на \`COMPLETED\` или \`FAILED\`

## Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функциональности
3. Внесите изменения
4. Напишите тесты
5. Отправьте pull request

## Решение проблем

При возникновении проблем:
1. Проверьте логи контейнеров: `docker-compose logs`
2. Убедитесь, что все сервисы запущены: `docker-compose ps`
3. Проверьте подключение к БД и RabbitMQ
